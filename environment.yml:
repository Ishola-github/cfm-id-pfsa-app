name: app
channels:
  - conda-forge
dependencies:
  # Pin Python to match your project
  - python=3.11

  # Core scientific stack from conda-forge (fast, compatible)
  - rdkit
  - numpy
  - pandas
  - scipy
  - scikit-learn
  - pip

  # (Optional) small utilities you prefer as conda packages
  # - matplotlib
  # - seaborn
